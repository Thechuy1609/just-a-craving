<%= content_for :title, "Contact Us" %>
<main class="py-10">
  <h1 class="text-5xl text-green-500 font-extrabold text-center">Leave us a message so we can get in touch!</h1>
  <div class="mt-10 sm:mx-auto sm:w-full sm:max-w-[480px]">
    <div class="bg-white px-6 py-12 shadow sm:rounded-lg sm:px-12">
      <%= render "form" %>
    </div>
  </div>
</main>
  